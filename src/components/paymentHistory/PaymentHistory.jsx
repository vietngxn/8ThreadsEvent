import styles from "./PaymentHistory.module.css";
export default function PaymentHistory() {
    return (
        <div>
            <span className={styles.title}>Lịch sử thanh toán</span>
        </div>
    );
}